# Layout Lab Guide

Choose Broadcast, Tactical, or Analysis presets. The same verified checkpoint state is rendered under each geometry. Diagnostics measure rendered card count, zone count, card overlap, viewport clipping, and viewport dimensions from DOM bounds.
