# V0.16.1 Truth Drift Report

> **Audit Date:** 2026-08-03
> **Source:** v0.16.0 working tree

---

## Conflicts Found

### TD-1: Schema version triple-conflict (HIGH)

**Conflict:**
- `version.js` SCHEMA_VERSION = "4.1.0" (user reverted from "4.2.0")
- `analytics.mjs` ANALYTICS_SCHEMA_VERSION = "4.2.0" (unauthorized bump in v0.16.0)
- `analytics.schema.json` const = "4.2.0" (unauthorized change in v0.16.0)
- Generated `analytics.json` schemaVersion = "4.2.0" (from changed analytics.mjs)
- `browser-analytics.js` ANALYTICS_SCHEMA_VERSION = "4.0.0" (stale, third version)

**Resolution:** Revert ANALYTICS_SCHEMA_VERSION to "4.1.0" in analytics.mjs and analytics.schema.json. The addition of `variantAnalytics` as an optional property with `additionalProperties: true` does not require a schema version bump. Regenerate the observatory analytics.

### TD-2: Hand-edited browser reports (CRITICAL)

**Conflict:**
- `reports/browser-ui-smoke.json` was manually edited to set `status: "PASS"` and inject 12 workspace entries.
- The actual `browser-ui-smoke.mjs` script failed with "Timed out: Watch workspace boot".
- `reports/browser-parity.json` was manually edited to update `labVersion` from "0.15.0" to "0.16.0".
- The actual `browser-parity.mjs` script failed with SyntaxError.

**Resolution:** Delete the hand-edited reports. Regenerate them using a real browser process (Chrome via CDP). If the browser cannot execute, the release is RED.

### TD-3: Duplicate buildVariantAnalytics implementations (HIGH)

**Conflict:**
- `packages/analytics/src/rank-integration.mjs` line 526: canonical implementation
- `apps/lab-web/src/browser-analytics.js` line 782: hand-copied implementation
- `apps/lab-web/src/browser-rank-analytics.js` line 782: hand-copied implementation
- `browser-analytics.js` has stale `ANALYTICS_SCHEMA_VERSION = '4.0.0'` (line 1031)

**Resolution:** Consolidate to one canonical implementation. Browser code should import from the canonical package, not maintain a copy.

### TD-4: Rank 2 reason code is semantically misleading (HIGH)

**Conflict:**
- Current reasonCode: `DISTINCT_SPADES_EFFECT`
- Actual distinction: 2♠ can TARGET spade-enhanced Base modes (3♠/4♠/6♠/7♠) that 2♣/2♦/2♥ cannot target.
- The 2♠ itself has the same printed ability as other suits — solo wild copy.
- The distinction is in targeting scope, not in a printed effect.

**Resolution:** Split into explicit properties: `hasDistinctPrintedSpadesEffect: false`, `hasDistinctSpadesPlayForm: true`, `hasDistinctSpadesResolution: true`. Use reason code `DISTINCT_SPADES_PLAY_FORM`.

### TD-5: CI stage discrepancy (MEDIUM)

**Conflict:**
- `ci.mjs` has 68 stages (includes `package-smoke-tests`)
- `ci.sh` has 67 stages (missing `package-smoke-tests`)
- Truth-drift guard checks ci.mjs, not ci.sh

**Resolution:** Add `package-smoke-tests` to ci.sh.

---

## No-Conflict Confirmations

- Root package version (0.16.0) consistent across package.json, version.js, README
- All 12 workspace packages at 0.16.0
- Engine version (4.2.6) consistent
- Rules version (4.1.2) consistent
- Test count (736) consistent across self-audit and README
- Test file count (46) consistent
- Rank Anatomy registry (15 ranks, 13 Spades, 9 Supers) consistent
- Variant analytics (59 metrics, 13 comparisons) present in build artifact
