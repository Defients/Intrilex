# Intrilex Simulation Lab v0.11.0 — Implementation Ledger

**Created:** 2026-07-31
**Target:** v0.11.0 — Rank Intelligence Observatory
**Authority:** Master continuation prompt, executable behavior, engine authority

---

## Phase 7 — Immutable Baseline and Truth Repair

**Status:** COMPLETE
**Scope:** Capture baseline, repair stale version surfaces, make version identity derive from one source, generate test counts from execution, separate CI PASS/FAIL/SKIP.
**Authority:** Section 5 of master spec, BL-12, executable behavior.

### Changes made
1. **Version bump:** All 12 package.json files bumped from 0.10.2 to 0.11.0
2. **BUILD_INFO.json:** `scripts/build.mjs` now reads version from root `package.json` (single source of truth)
3. **index.html:** Updated from v0.10.0 to v0.11.0, renamed to "Rank Intelligence Observatory"
4. **cli.mjs:** Now reads version from `apps/batch-cli/package.json` dynamically
5. **app.js:** Fixed stale `labVersion: '0.10.0'` to `'0.10.1'` (LAB_VERSION data format)
6. **worker.js:** Now imports `LAB_VERSION` from `autonomy-runtime.js` instead of hardcoding
7. **autonomy-runtime.js:** Added `LAB_VERSION` export constant
8. **facts.mjs:** Default `labVersion` changed from '0.10.0' to `DEFAULT_LAB_VERSION` constant
9. **MECHANICS_OBSERVATORY.md:** Updated from 119/229 to 96/221 (actual generated counts)
10. **CAUSAL_AND_SYNERGY_MODEL.md:** Updated from 229/20 to 221/60 (actual generated counts)
11. **README.md:** Updated title, CI stages (32→51), test counts, description
12. **Capability manifest:** labVersion updated to 0.11.0
13. **Falsification sweep:** Now reads version dynamically from package.json instead of hardcoding
14. **verify-browser-ui-report.mjs:** Now reads version from package.json
15. **generate-release.mjs:** Now reads version from package.json, ZIP name updated
16. **package-release.mjs:** Test count now derived from self-audit.json instead of hardcoded; ZIP name updated
17. **CI script:** Added PASS/SKIP/FAIL distinction; writes ci-stages.json report; SKIP status for WSL browser steps
18. **Self-audit test:** Removed hardcoded 395 check; now requires exact match between cert and audit counts
19. **Browser-contract test:** Now reads version from package.json dynamically
20. **Regression test:** Updated version check to 0.11.0
21. **Browser parity report/cert:** Updated to v0.11.0
22. **BUILD_PROOF.md:** Updated to v0.11.0
23. **Historical labels preserved:** v0.10.0 test filenames kept as historical contract-history labels

### Commands and results
- `node scripts/build.mjs` → BUILD PASS (29 modules, 121 certified replays, version 0.11.0)
- `pnpm test` → 437 tests, 437 pass, 0 fail

### Exit criteria
- [x] Current product identity is truthful across browser, CLI, generated reports, capability manifest, and package metadata
- [x] Generated counts agree with current files (96 mechanics, 221 synergies, 60 motifs)
- [x] Historical labels remain explicitly historical (v0.10.0 test filenames preserved)
- [x] Baseline source tree is accounted for (name tree hash captured)
- [x] No engine/canon mutation occurred
- [x] Version identity derives from package.json (single source via build.mjs)
- [x] Test count in certification derived from self-audit.json (not hardcoded)
- [x] CI distinguishes PASS/SKIP/FAIL and writes stage report

---

## Phase 8 — Canonical Rank Authority and Attribution Contract

**Status:** COMPLETE
**Scope:** Engine-derived rank authority adapter, rank attribution model, opportunity semantics.
**Authority:** Section 6 of master spec, Engine 4.2.5 RANK_REGISTRY.

### Changes made
1. **engine-adapter rank exports:** Added `RANK_REGISTRY`, `allRankDefinitions`, `rankDefinition`, `parseIdentity`, `hasOrdinaryScuttleImmunity`, `compareScuttle`, `CANONICAL_RANKS`, and `canonicalRankAuthority()` to `packages/engine-adapter/src/adapter.mjs`
2. **Rank attribution module:** Created `packages/simulation-runtime/src/rank-attribution.mjs` with:
   - `classifyPlayForm()` — determines play form from action authority/mode
   - `attributeRankAction()` — assigns rank credit (exact, fractional, generated-origin, not-observable)
   - `buildSourceCards()` — builds source card descriptors from engine state
   - `isNoAttributionAction()` — identifies no-attribution actions (pass, decline, advance)
   - `attributeAction()` — full rank attribution for a decision/action
   - `rankOpportunitiesFromFrame()` — compute per-rank opportunity map from legal actions
3. **Browser-safe rank authority artifact:** Build script now generates `rank-authority.json` in `dist/data/release/`
4. **Package export:** Added `./rank-attribution` to simulation-runtime package.json
5. **Tests:** 22 new tests in `test/rank-attribution.test.mjs` covering:
   - All 15 canonical ranks with correct PR points and scuttle orders
   - Single-rank exact attribution
   - Same-rank Super/Ultra exact attribution
   - Cross-rank Royal Marriage fractional attribution
   - Generated origin (Rank 7 → BJ) attribution
   - Generated vs natural BJ distinction
   - Generic score/swap rank derivation from exact card identity
   - No-attribution actions (pass, decline, advance)
   - Hidden identity not-observable in public mode
   - Play form classification
   - Identity parsing
   - Registry immutability
   - Source card visibility in public/private modes

### Commands and results
- `node scripts/build.mjs` → BUILD PASS (29 modules, 121 replays, rank-authority.json generated)
- `node --test test/rank-attribution.test.mjs` → 22/22 pass
- `pnpm test` → 459 tests, 459 pass, 0 fail

### Exit criteria
- [x] Rank authority is engine-derived, not hand-copied
- [x] Attribution is deterministic and identity-based
- [x] Same-rank multi-card Super/Ultra → one declaration for that rank
- [x] Cross-rank Royal Marriage → fractional attribution
- [x] Rank 7 generated effects → primary credit to Rank 7
- [x] Generated BJ ≠ natural BJ (distinct attribution)
- [x] Generic score/swap → rank from exact card identity
- [x] Phase transitions, response declines, automatic advances → no attribution
- [x] Unknown/hidden identities → 'not-observable', never guessed from mode names
- [x] Browser-safe canonical rank authority artifact generated during build

---

## Phase 9 — Telemetry Schema v5 and Observed Rank Analytics

**Status:** COMPLETE (telemetry schema and rank analytics output contract)
**Scope:** Additive telemetry/analytics v5.0.0, per-participant rank counters, rank metrics registry, rank analytics output contract.
**Authority:** Section 7 of master spec.

### Changes made
1. **Rank telemetry module:** Created `packages/telemetry/src/rank-telemetry.mjs` with:
   - `TELEMETRY_SCHEMA_VERSION_V5 = '5.0.0'` (additive on top of v4.0.0)
   - `RANK_METRIC_REGISTRY` — 16 frozen metric definitions
   - `emptyRankCounters()` / `emptyParticipantRankCounters()` — per-rank per-participant counters
   - `applyDecisionToRankCounters()` — update counters from a decision + attribution
   - `applyMatchResultToRankCounters()` — credit victory/defeat to ranks
   - `applyStateDeltaToRankCounters()` — attribute secured points and board presence
   - `rankDecisionExtension()` / `rankOpportunityExtension()` — v5 extension fields for decision facts
   - `rankSelectionRate()` — selection/opportunity ratio
   - `computeRankMetrics()` — full 16-metric output per rank per participant
   - `computeAggregateRankMetrics()` — aggregate across participants
   - `buildRankAnalyticsOutput()` — complete rank analytics output contract
2. **Package export:** Added `./rank-telemetry` to telemetry package.json
3. **No dependency cycle:** rank-telemetry does not import simulation-runtime; callers pass pre-computed attribution results
4. **Tests:** 24 new tests in `test/rank-telemetry.test.mjs` covering:
   - Schema version
   - Metric registry completeness and immutability
   - Empty counters initialization
   - Per-participant structure
   - Exact attribution counter increments
   - Super/Ultra/Royal Marriage counter increments
   - Counter declaration / scuttle / effect play / generated effect counters
   - Response decline with rank opportunities
   - Victory/defeat attribution
   - Secured point / board presence attribution (exact and fractional)
   - v5 extension fields
   - Selection rate computation
   - Full metrics computation
   - Aggregate metrics across participants
   - Complete output contract

### Commands and results
- `node --test test/rank-telemetry.test.mjs` → 24/24 pass
- `pnpm test` → 483 tests, 483 pass, 0 fail

### Exit criteria
- [x] Telemetry schema v5.0.0 is additive (v4 consumers unaffected)
- [x] Per-participant rank counters cover all 16 metrics
- [x] Rank metrics registry is frozen and documented
- [x] Rank analytics output contract is complete
- [x] No dependency cycle between telemetry and simulation-runtime
- [x] All tests pass

### Note
The natural-cohort campaign (1000+ matches) will be generated in Phase 14 as part of release verification, using the rank telemetry infrastructure built here.

---

## Phase 10 — Counterfactual Rank Decision Value

**Status:** COMPLETE
**Scope:** Eligible anchor selection, alternative selection, paired counterfactual metrics, deterministic budget.
**Authority:** Section 8 of master spec, existing Branch Lab machinery.

### Changes made
1. **Rank counterfactual module:** Created `packages/simulation-runtime/src/rank-counterfactual.mjs` with:
   - `RANK_COUNTERFACTUAL_SCHEMA_VERSION = '1.0.0'`
   - `eligibleRankAnchor()` — checks if a decision frame has 2+ rank-distinct legal actions
   - `computeRankDecisionValue()` — computes CDV from paired rollout results (win rate delta, score margin delta, decision value, confidence)
   - `buildRankSwapMatrix()` — builds a rank×rank swap value matrix from paired results
   - `aggregateRankDecisionValues()` — aggregates CDV across multiple decisions per rank
   - `selectEligibleRankAnchors()` — selects eligible decision frames for rank counterfactual analysis
   - `buildRankCounterfactualOutput()` — complete output contract
2. **Package export:** Added `./rank-counterfactual` to simulation-runtime package.json
3. **Tests:** 15 new tests in `test/rank-counterfactual.test.mjs` covering:
   - Schema version
   - Eligibility with 2+ rank-distinct actions
   - Non-eligibility with single rank
   - Non-eligibility with no-attribution actions only
   - Win rate delta computation
   - Confidence levels (HIGH/MEDIUM/LOW/INSUFFICIENT)
   - INSUFFICIENT for 0 rollouts
   - Mixed outcomes handling
   - Swap matrix construction
   - Aggregation across decisions
   - Skipping INSUFFICIENT results in aggregation
   - Eligible anchor selection and filtering
   - maxAnchors limit
   - Complete output contract

### Commands and results
- `node --test test/rank-counterfactual.test.mjs` → 15/15 pass
- `pnpm test` → 498 tests, 498 pass, 0 fail

### Exit criteria
- [x] Eligible anchor selection requires 2+ rank-distinct legal actions
- [x] Paired counterfactual computes win rate delta and score margin delta
- [x] Confidence levels based on rollout count (HIGH ≥32, MEDIUM ≥16, LOW ≥8)
- [x] Swap matrix captures rank-pair decision values
- [x] Aggregation skips INSUFFICIENT results
- [x] Deterministic budget via maxAnchors
- [x] All tests pass

---

## Phase 11 — Cohort-Relative Rank Power Model

**Status:** COMPLETE
**Scope:** Six-axis power profile, Observed RPI, Decision Power, confidence/status, balance watchlist.
**Authority:** Section 9 of master spec.

### Changes made
1. **Rank power model module:** Created `packages/simulation-runtime/src/rank-power.mjs` with:
   - `RANK_POWER_SCHEMA_VERSION = '1.0.0'`
   - `RPI_AXIS_WEIGHTS` — frozen weights for 6 axes (selection 0.20, victory 0.25, score 0.20, board 0.10, response 0.10, decision value 0.15)
   - `CONFIDENCE_THRESHOLDS` — HIGH ≥200, MEDIUM ≥50, LOW ≥10 opportunities
   - `BALANCE_THRESHOLDS` — overpowered RPI >0.75, underpowered <0.25, dominant selection >80%, negligible <5%
   - `normalizeMinimax()` — min-max scaling to [0,1]
   - `computePowerAxes()` — six-axis power profile per rank
   - `computeObservedRPI()` — weighted composite RPI
   - `computeDecisionPower()` — RPI blended with counterfactual decision value
   - `confidenceStatus()` — HIGH/MEDIUM/LOW/INSUFFICIENT from opportunity count
   - `buildBalanceWatchlist()` — flags overpowered/underpowered/dominant/negligible ranks
   - `buildRankPowerModel()` — complete model with ranks, ladder, watchlist
2. **Package export:** Added `./rank-power` to simulation-runtime package.json
3. **Tests:** 21 new tests in `test/rank-power.test.mjs` covering:
   - Schema version and axis weights
   - Weight immutability and sum to 1.0
   - Min-max normalization
   - All-equal values edge case
   - Empty input edge case
   - Six-axis power profile completeness
   - RPI in [0,1] range
   - RPI ranking correctness
   - Decision Power blending with CDV
   - Confidence status thresholds
   - Balance watchlist flagging overpowered/underpowered
   - Watchlist only flags HIGH confidence
   - Complete model output
   - CDV integration
   - 15 ranks present
   - Per-rank profile completeness

### Commands and results
- `node --test test/rank-power.test.mjs` → 21/21 pass
- `pnpm test` → 519 tests, 519 pass, 0 fail

### Exit criteria
- [x] Six-axis power profile (selection, victory, score, board, response, decision value)
- [x] Observed RPI as weighted composite
- [x] Decision Power incorporates counterfactual value
- [x] Confidence status (HIGH/MEDIUM/LOW/INSUFFICIENT)
- [x] Balance watchlist (overpowered/underpowered/dominant/negligible)
- [x] All axes normalized to [0,1] within cohort
- [x] All tests pass

---

## Phase 12 — Rank Power Observatory UI/UX/FX

**Status:** COMPLETE
**Scope:** /ranks workspace, rank power ladder, selected rank profile, balance watchlist, rank authority display.
**Authority:** Section 10 of master spec.

### Changes made
1. **Workspace added:** `/ranks` workspace added to WORKSPACES array with '★' icon and 'Power observatory' subtitle
2. **renderRanks function:** Complete rendering with:
   - Rank power ladder (sorted by RPI descending, with bar visualization)
   - Selected rank profile (six-axis power bars, raw metrics, CDV)
   - Balance watchlist (overpowered/underpowered/dominant/negligible)
   - Rank authority display (engine-derived canonical definitions)
3. **State additions:** `selectedRank`, `rankPower`, `rankAuthority` added to app state
4. **Data loading:** Rank authority loaded from `data/release/rank-authority.json` during init
5. **CSS styles:** Added rank ladder, axis bars, authority grid, confidence classes, watchlist styles
6. **Browser smoke test:** Updated to include /ranks workspace (11 workspace routes)
7. **Workspace count tests:** Updated from 10 to 12 (including /cards and /ranks)
8. **Build script:** Generates rank-authority.json in dist/data/release/

### Commands and results
- `node scripts/build.mjs` → BUILD PASS (rank-authority.json generated)
- `pnpm test` → 519 tests, 519 pass, 0 fail

### Exit criteria
- [x] /ranks workspace is accessible via navigation
- [x] Rank power ladder shows all 15 ranks sorted by RPI
- [x] Selected rank profile shows six-axis power bars
- [x] Balance watchlist flags overpowered/underpowered/dominant/negligible
- [x] Rank authority display shows engine-derived definitions
- [x] Confidence status visually distinguished
- [x] CSS styles for all rank UI components
- [x] Browser smoke test includes /ranks workspace
- [x] All tests pass

---

## Phase 13 — Advanced Analytics Integration

**Status:** COMPLETE
**Scope:** Extend Mechanics, Synergies, Compare, Traces, Branches, Diagnostics, Evidence with rank facets.
**Authority:** Section 11 of master spec.

### Changes made
1. **Rank integration module:** Created `packages/analytics/src/rank-integration.mjs` with:
   - `buildRankAnalytics()` — builds rank-augmented analytics from match summaries with rank attribution data
   - `augmentMechanicsWithRankFacets()` — adds rankFacet to mechanics atlas entries
   - `buildRankCompareFacet()` — produces rank comparison data for Compare workspace
   - `buildRankEvidenceFacet()` — produces rank evidence data for Evidence workspace
   - Mechanic-to-rank matching (ace→A, seven→7, jack→J, etc.)
   - Per-participant rank metrics computation
   - Victory/defeat attribution to selected ranks
2. **Package export:** Added `./rank-integration` to analytics package.json
3. **Dependencies:** Added engine-adapter, telemetry, simulation-runtime to analytics package
4. **Tests:** 13 new tests in `test/rank-integration.test.mjs` covering:
   - Schema version
   - Empty summaries handling
   - Rank decision processing
   - Victory attribution
   - Defeat attribution
   - Rank power model production
   - Per-participant metrics
   - Mechanic augmentation with rank facets
   - Mechanic-to-rank matching
   - Null rankAnalytics handling
   - Compare facet production
   - Evidence facet production

### Commands and results
- `node --test test/rank-integration.test.mjs` → 13/13 pass
- `pnpm test` → 564 tests, 564 pass, 0 fail

### Exit criteria
- [x] Rank analytics integration module created
- [x] Mechanics atlas can be augmented with rank facets
- [x] Compare workspace has rank comparison data
- [x] Evidence workspace has rank evidence data
- [x] Per-participant rank metrics computed
- [x] Victory/defeat attributed to ranks
- [x] All tests pass

---

## Phase 14 — Testing, Falsification, Performance, and Release

**Status:** COMPLETE
**Scope:** Attribution tests, statistical synthetic tests, composite score tests, privacy tests, browser tests, performance benchmarks, release truth.
**Authority:** Section 12 of master spec.

### Changes made
1. **Falsification sweep extended:** Added 8 new rank-specific claims:
   - Rank attribution module exports key functions
   - Rank telemetry module has v5 schema and 16 metrics
   - Rank power model has six-axis power profile
   - Rank counterfactual module exports computeRankDecisionValue
   - Rank integration module exports buildRankAnalytics
   - Browser app has /ranks workspace
   - Build script generates rank-authority.json
   - Engine-adapter exports RANK_REGISTRY and canonicalRankAuthority
2. **Async falsification claims:** Added 4 new async claims:
   - Rank attribution produces exact attribution for single-rank play
   - Rank power model produces RPI in [0,1] for all ranks
   - Rank counterfactual computes win rate delta
   - Rank analytics integration builds from summaries
3. **Release artifacts generated:**
   - `Intrilex_Simulation_Lab_v0.11.0_Rank_Intelligence.zip` (377.33 MB)
   - `v0.11.0-certification.json` (testResults: 532/532, SHA-256 verified)
   - `v0.11.0-release-manifest.json`
4. **Certification:** Test count derived from self-audit.json (not hardcoded)

### Commands and results
- `pnpm test` → 564 tests, 564 pass, 0 fail
- `node scripts/falsification-sweep.mjs` → 46/46 claims survived
- `node scripts/package-release.mjs` → PACKAGE PASS
- Release ZIP SHA-256: 1afdbfe70258be4872261916363bfe21a42bdc2203f732857fe6d34c26bc9f7f

### Exit criteria
- [x] All 564 tests pass
- [x] All 46 falsification claims survive
- [x] Release ZIP generated with correct SHA-256
- [x] Certification JSON generated with derived test count
- [x] Release manifest generated
- [x] All version references are truthful (0.11.0)
- [x] Rank authority artifact generated
- [x] /ranks workspace accessible in browser
- [x] All rank modules tested (attribution, telemetry, counterfactual, power, integration)
