> **⚠ TRUTH QUARANTINE (Phase 0):** This report was produced by the pre-Cascade or
> in-Cascade validator which contained the Section 3.3 anchor inversion
> (decisionId re-derivation + actor/seat presence-only check). Its PASS verdicts
> on counterfactual anchor binding are **HISTORICAL/PROVISIONAL** and not
> authoritative. The authoritative baseline is `Intrilex(4-5).zip`
> (SHA-256 `CCC2D4AF...AFD9`), verified at commit `81ce617`. The anchor inversion
> was fixed at commit `8f239ee`. See `V0.10.0_IMPLEMENTATION_LEDGER.md`.

# V0.10.0 Forensic Reconciliation — current2 Baseline

## Environment

- Node: v22.14.0
- npm: 10.9.2
- pnpm: 11.13.0
- OS: Windows

## Package Versions

All workspace packages report v0.10.0.

## Dependency Graph (Current)

```
shared ← policy-sdk ← policies ← game-adapter
                     ← policies ← game-ai ← decision-intelligence ← simulation-runtime ← analytics
                                                   ↑___________________|
```

### Active Cycle

`decision-intelligence` → `simulation-runtime` (via `runPolicyMatch` import in counterfactual.mjs)
`simulation-runtime` → `decision-intelligence` (via dependency in package.json)
`simulation-runtime` → `analytics`
`analytics` → `decision-intelligence`

pnpm reports: cyclic workspace dependencies: analytics, decision-intelligence, simulation-runtime

## Lockfile Status

`pnpm install --offline --frozen-lockfile` FAILS:
- `packages/policies/package.json` removed `@intrilex/game-ai` dependency but lockfile still references it
- Outdated lockfile cannot install with frozen-lockfile

## Test Baseline

- 197 tests, 196 pass, 1 fail
- Failing: `capability report supports bounded Advanced Core without claiming complete Core` — expects labVersion `0.9.0`, manifest now says `0.10.0`
- FIX APPLIED: Updated test expectation to `0.10.0`

## Schema Version Drift

| Component | Source Schema | Dist Schema |
|-----------|--------------|-------------|
| Decision Trace | 2.0.0 | 1.0.0 |
| Counterfactual | 2.0.0 | 1.0.0 |
| Policy Diagnostics | 2.0.0 | 1.0.0 |

Browser dist `decision-intelligence.js` is schema 1.0.0 — fabricated replay, wrong seed derivation, no checkpoint restoration.

## Version String Drift

| Location | Version |
|----------|---------|
| Root package.json | 0.10.0 |
| README | 0.8.0 |
| CLI help | 0.8.0 |
| package-lock.json | 0.8.0 |
| campaign.mjs labVersion | 0.10.0 (FIXED from 0.9.0) |
| build.mjs | 0.10.0 (FIXED from 0.9.0) |
| capability-manifest.json | 0.10.0 (FIXED from 0.9.0) |
| BUILD_INFO.json | 0.10.0 |
| browser parity report | 0.8.0 |
| browser UI verifier | 0.8.0 |

## Findings Summary

### 5.1 Partial Improvements Preserved
- Counterfactual accepts replay and can restore state ✓
- Continuation seed excludes alternativeActionId ✓
- Runtime accepts initialState ✓
- Decision traces consume policy-emitted candidate totals ✓
- Uniform-random traces use null margin ✓
- Quick audits inspect actor vs active player ✓
- Browser worker accepts decision array ✓
- HYBRIX choose() applies card-game cognition ✓
- Worker threads isolate match throws ✓ (FIXED in prior session)
- Core policies don't import HYBRIX from primary index ✓

### 5.2 Browser Branch Lab Fabricated — CONFIRMED
- Dist DI is schema 1.0.0
- Uses fabricated one-command replay
- Derives seeds using alternativeActionId
- Never loads selected replay, never restores checkpoint
- Reports completed policy-conditioned estimate

### 5.3 Node Counterfactual Anchoring Incomplete — CONFIRMED
- No matchId validation
- No checkpointHash validation (accepts wrong hash)
- Ignores decisionIndex
- No decision ID binding
- No command hash binding
- No legal-action-set hash verification
- No originally selected action verification
- Filters out failed rollouts
- Uses hardcoded Seat 1 in comparison
- Reports absolute score margins, not focal-seat utility

### 5.4 Contract Tests Contain False Positives — CONFIRMED
- Assertions wrapped in `if (result.status === 'COMPLETED')`
- "Alternative changes state" test reuses selected action
- HYBRIX cognition tests merely prove match terminates
- Spatial vocabulary tests only confirm choose is a function
- Hidden information tests only repeat same ordinary match
- Quick tests do nothing when no violating fixture found

### 5.5 Decision Evidence Partly Reconstructed — CONFIRMED
- Score components are generic reconstructed values
- Reconstructed components displayed beside actual totals
- Only first 8 candidate scores preserved (agent.mjs:288)
- No modifier contributions preserved
- No uniform-random probability
- No evidence limitations in trace

### 5.6 Rule Audits Incomplete — CONFIRMED
- Exhausted Pass marked PASS without proving all conditions
- `exhausted-pass-forced` check: `status: 'PASS', observed: true` — no evidence
- UI reads `check`, source emits `checkId` — display drift
- NOT_APPLICABLE and unavailable inconsistently represented

### 5.7 Browser Diagnostics Zero Evidence — CONFIRMED
- App sends `state.observatory?.decisions ?? []`
- Observatory artifact has no `decisions` field
- Real workflow supplies empty array

### 5.8 HYBRIX Why-Traces Lost — CONFIRMED
- agent.choose() emits `metadata.hybrixTrace`
- Runtime preserves only reasonCode and candidateScores
- Trace adapter reconstructs generic TACTICAL trace
- `reconstructed: true` on HYBRIX traces

### 5.9 HYBRIX Not Fully Domain-Native — CONFIRMED
- Initializes perception, spatial cognition, coordination, LOD/failsafe
- Trace emits `LONE_WOLF`, `coordinationRole`
- References `own.deckCount` instead of draw pile count

### 5.10 HYBRIX Run Isolation Broken — CONFIRMED
- Agent cache keyed by `matchId:actorId` only
- Identical match configs regenerate same match ID
- Cache cleared only when matchId changes
- No runInstanceId concept

### 5.11 Benchmark Statistics Invalid — CONFIRMED
- p1Wins/p2Wins treated as named-policy wins (they're seat counters)
- Seat swaps misattribute wins
- Self-play produces 100% HYBRIX win rates
- Overall aggregate mixes baseline and self-play
- Errors counted as draws
- UNSUPPORTED_CONFIGURATION folded into draws

### 5.12 Dependency/Installation Broken — CONFIRMED
- Frozen pnpm install fails (lockfile outdated)
- pnpm reports cycle: analytics, decision-intelligence, simulation-runtime
- policies/hybrix imports game-ai without declaring dependency

### 5.13 Release Artifacts Mix Four Truth Eras — CONFIRMED
- See version drift table above

### 5.14 Certification Not Green — CONFIRMED
- 1 test failure (FIXED)
- pnpm frozen install fails
- Browser verifiers expect 0.8.0
- No valid v0.10.0 release archive
