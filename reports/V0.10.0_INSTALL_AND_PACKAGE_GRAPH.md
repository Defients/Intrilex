# V0.10.0 Install and Package Graph

## Package Manager

**Canonical manager:** pnpm  
**Declared in:** `package.json` → `"packageManager": "pnpm@10.11.0"`  
**Lockfile:** `pnpm-lock.yaml` (authoritative)  
**Removed:** `package-lock.json` (was stale at v0.8.0, unsupported workspace: protocol)  
**Do not ship:** `node_modules/` excluded from release ZIP

## Clean Install

```bash
pnpm install --offline --frozen-lockfile
```

This is the first CI gate and extracted-release gate.

## Package Graph

12 packages, zero cycles (verified by `check:package-graph`):

```
@intrilex/analytics -> [@intrilex/shared, @intrilex/decision-intelligence]
@intrilex/batch-cli -> [@intrilex/engine-adapter, @intrilex/policies, @intrilex/simulation-runtime]
@intrilex/browser-crypto-shim -> [none]
@intrilex/decision-intelligence -> [@intrilex/shared]
@intrilex/engine-adapter -> [@intrilex/shared]
@intrilex/game-ai -> [@intrilex/policy-sdk, @intrilex/policies, @intrilex/decision-intelligence]
@intrilex/policies -> [@intrilex/policy-sdk]
@intrilex/policy-sdk -> [@intrilex/shared]
@intrilex/shared -> [none]
@intrilex/simulation-runtime -> [@intrilex/shared, @intrilex/telemetry, @intrilex/engine-adapter, @intrilex/policy-sdk, @intrilex/policies, @intrilex/game-ai, @intrilex/decision-intelligence, @intrilex/analytics]
@intrilex/telemetry -> [@intrilex/shared]
intrilex-simulation-lab -> [all workspace packages]
```

## Package Boundaries

| Package | Role |
|---------|------|
| `shared` | Canonical hashing and pure primitives |
| `policy-sdk` | Contracts, deterministic RNG, lifecycle contracts |
| `policies` | Baseline Intrilex policies |
| `engine-adapter` | Sole engine-authority adapter |
| `decision-intelligence` | Schemas, validation, comparison, diagnostics, statistics |
| `game-ai` | Intrilex-native HYBRIX |
| `simulation-runtime` | Orchestration and continuation execution |
| `analytics` | Pure evidence aggregation |
| `telemetry` | Semantic telemetry frames |
| `browser-crypto-shim` | Browser crypto compatibility |
| `batch-cli` | Command-line interface |
| `intrilex-simulation-lab` | Root workspace |

## Changes Made

- Added `packageManager: "pnpm@10.11.0"` to root `package.json`
- Removed stale `package-lock.json` (v0.8.0, unsupported workspace: protocol)
- Updated `README.md` from v0.8.0 to v0.10.0 with pnpm commands
- Updated CLI help from v0.8.0 to v0.10.0
- Updated `verify-browser-ui-report.mjs` version check from 0.8.0 to 0.10.0, workspace count from 6 to 10
- Updated `verify-extracted.mjs` from npm to pnpm commands
- Updated `package-release.mjs` to exclude `package-lock.json` from ZIP
- Updated CI script to include behavioral test suite
