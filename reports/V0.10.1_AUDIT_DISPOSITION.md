# Intrilex Simulation Lab v0.10.1 — Audit Disposition Ledger

**Generated:** 2026-07-31
**Last updated:** 2026-07-31 (all claims resolved)
**Authority hierarchy:** (1) User prompt → (2) Rules v4.1.2 → (3) Engine v4.2.5 → (4) Executable behavior → (5) Source contracts → (6) Tests → (7) Generated reports → (8) Attached audits

## Resolution Summary

All 21 inherited audit claims have been investigated and resolved. The original dispositions (CONFIRMED/PARTIALLY CONFIRMED/REQUIRES PROBE) have been updated with resolution status:

| # | Claim | Original | Resolution |
|---|---|---|---|
| 1 | 337 tests, 3 fail | CONFIRMED | **RESOLVED** — 416 tests, 0 fail |
| 2 | 380 pass, 3 fail across all files | CONFIRMED | **RESOLVED** — all pass |
| 3 | Stale Branch Lab + Diagnostics + smoke count | CONFIRMED | **RESOLVED** — behavioral tests + 10 workspaces |
| 4 | 2 omitted anchor suites | CONFIRMED | **RESOLVED** — included in default suite + CI |
| 5 | Package graph 12/0 cycles | CONFIRMED | **PRESERVED** — still 12/0 |
| 6 | Build passes | CONFIRMED | **PRESERVED** — still passes |
| 7 | release/ absent | CONFIRMED | **RESOLVED** — release packaging works |
| 8 | Stale browser proof verifiers | CONFIRMED | **RESOLVED** — dynamic BUILD_INFO |
| 9 | Draw-pile identity leak | CONFIRMED | **RESOLVED** — views.ts hides DP + 12-test matrix |
| 10 | Ordered draw-pile identities in artifacts | CONFIRMED | **RESOLVED** — artifacts regenerated |
| 11 | Vacuous privacy selectors | CONFIRMED | **RESOLVED** — structural privacy-matrix test |
| 12 | HYBRIX cross-run leakage | PARTIALLY CONFIRMED | **RESOLVED** — runInstanceId + 10-test envelope |
| 13 | Campaign PASS when all aborted | CONFIRMED | **RESOLVED** — three-state PASS/PARTIAL/FAIL |
| 14 | HYBRIX accounting invariant false | CONFIRMED | **RESOLVED** — invariant true, non-zero exit |
| 15 | Browser 5 vs Node 19 policies | CONFIRMED | **RESOLVED** — all 19 policies in browser |
| 16 | CI stage count mismatch | CONFIRMED | **RESOLVED** — 48 stages |
| 17 | Default test omits anchor suites | CONFIRMED | **RESOLVED** — all suites included |
| 18 | CI verifies stale browser reports | CONFIRMED | **RESOLVED** — CI generates then verifies |
| 19 | Smoke JSON/MD/test disagreement | CONFIRMED | **RESOLVED** — all agree on 10 |
| 20 | Competing release workflows | CONFIRMED | **RESOLVED** — package-release.mjs works |
| 21 | Self-audit PASS/94 with false gates | CONFIRMED | **RESOLVED** — v2.0.0 with all gates true |

## Inherited audit claims — disposition

### Claim 1: `pnpm test` exercised 337 tests: 334 passed, 3 failed

**Disposition:** CONFIRMED
**Evidence:** `pnpm test` produced `# tests 337, # pass 334, # fail 3`, exit code 1. Failures: Browser Branch Lab (135), Browser Diagnostics (136), Adversarial: Browser Evidence (150).

### Claim 2: Running all 23 test files explicitly produced 380 passes and 3 failures

**Disposition:** CONFIRMED
**Evidence:** Default suite 334 pass + 3 fail = 337. Anchor authority 39/39 pass. Browser anchor parity 7/7 pass. Total: 380 pass, 3 fail, 383 tests.

### Claim 3: Three failures were stale Branch Lab and Diagnostics "unavailable" expectations plus incomplete browser smoke workspace count

**Disposition:** CONFIRMED
**Evidence:**
- Test 135: "Branch Lab must show unavailable state" — stale assertion reads app.js source text, selector does not match actual rendered DOM.
- Test 136: "Diagnostics must show unavailable state" — same stale pattern.
- Test 150: "Must have 10 workspaces, got 7" — `reports/browser-ui-smoke.json` has 7 workspace keys, test expects 10.

### Claim 4: Two omitted v0.10.0 anchor suites passed 46/46 when invoked directly

**Disposition:** CONFIRMED
**Evidence:** `v0.10.0-anchor-authority.test.mjs` = 39/39 pass. `v0.10.0-browser-anchor-parity.test.mjs` = 7/7 pass. Total 46/46.

### Claim 5: Package graph check passed with 12 packages and zero cycles

**Disposition:** CONFIRMED
**Evidence:** `node scripts/check-package-graph.mjs` → "PACKAGE_GRAPH_ACYCLIC: 12 packages, zero cycles", exit 0.

### Claim 6: Full build passed including engine patch, capability manifest, 121 certified replay checks, 31 Advanced Core artifacts, observatory analytics, browser bundle

**Disposition:** CONFIRMED
**Evidence:** `pnpm run build` exit 0. Output: ENGINE PATCH BUILD PASS, CAPABILITY MANIFEST PASS, 121 certified replays, 31 advanced core artifacts, observatory analytics (matches=100, mechanics=91, synergies=222), BUILD PASS (browserModules=29, certifiedReplays=121).

### Claim 7: `release/` and `release/RELEASE_MANIFEST.json` were absent; manifest verification and release packaging failed

**Disposition:** CONFIRMED
**Evidence:** `Test-Path release` = False. `node scripts/manifest.mjs verify` → ENOENT `release/RELEASE_MANIFEST.json`. `node scripts/package-release.mjs` → ENOENT scandir `release`.

### Claim 8: Browser UI proof and browser proof verification were stale/incomplete; at least one verifier still expected Engine 4.2.4 / Rules 4.1.1

**Disposition:** CONFIRMED
**Evidence:** `scripts/verify-browser-proof-reports.mjs` line 7: `parity.engineVersion!=='4.2.4'||parity.rulesVersion!=='4.1.1'` — stale. Also expects `ui.campaign?.completedMatchCount` which does not exist in the smoke report schema. `scripts/verify-browser-ui-report.mjs` expects 10 workspaces but smoke generates 7.

### Claim 9: Draw Pile is canonically face-down but browser renders every draw-pile card identity

**Disposition:** CONFIRMED
**Evidence:** `runtime/autonomy-engine-dist/src/views.js` `publicStateView()` and `privateStateView()` filter hidden cards by `card.zone.endsWith("_HAND")` but do NOT include draw-pile zone (`DP`/`dp`). Public artifact `sample-data/autonomy/lab-replays/public/M-09782322...json` shows `identity: "Q"` for DP-zone cards. Browser `cardToken()` renders `card.identity` without role filtering.

### Claim 10: Public and player-authorized replay artifacts contained ordered draw-pile identities

**Disposition:** CONFIRMED
**Evidence:** Public artifact: DP cards have `PUB-` handles but `identity` field shows real values (Q, Q, 6, 4, 5). Authorized artifact `playerViews.P1`: DP cards show full IDs (CORE-015) and real identities (Q). Player view also leaks `rng: {seed: 784256783, cursor: 53}`.

### Claim 11: Existing smoke check for hidden opponent hand appeared vacuous because selectors did not match actual DOM

**Disposition:** CONFIRMED
**Evidence:** `test/privacy.test.mjs` checks for `"C-\d{3}"` pattern but card identities are single letters (Q, 6, A, K). `test/hidden-info.test.mjs` only checks CT-120 which has an empty draw pile. Neither test would catch the confirmed DP identity leak.

### Claim 12: HYBRIX singleton agents cached by (runInstanceId, matchId, actorId) while runtime did not pass unique run instance

**Disposition:** PARTIALLY CONFIRMED — requires deeper probe
**Evidence:** Falsification sweep claims `runInstanceId isolates agent cache across runs` and `Four identical HYBRIX runs produce one semantic result hash` pass. However, the full evidence-envelope comparison (first-decision scores, memory accuracy, decision hashes, trace hashes) is not yet tested. Phase 3 will add the complete repeat/interleave/worker/order matrix.

### Claim 13: Node campaign could report PASS and increment completedCount even when every match terminated aborted/DECISION_LIMIT

**Disposition:** CONFIRMED
**Evidence:** `packages/simulation-runtime/src/campaign.mjs:83-87`: `completedCount: successes.length` (all non-error), `campaignStatus: failures.length === 0 ? 'PASS' : 'FAIL'`. Aborted/unsupported matches are in `successes`, not `failures`.

### Claim 14: HYBRIX benchmark report violated its own accounting invariant while script exited zero

**Disposition:** CONFIRMED
**Evidence:** `reports/hybrix-benchmark.json` totals: `accountingInvariant: false` (1305+1267+231+0+6=2809 ≠ 3600). `scripts/benchmark-hybrix.mjs:238` computes the invariant but never checks it or calls `process.exit(1)`.

### Claim 15: Browser exposed five baseline policies while Node exposed five baseline plus fourteen HYBRIX

**Disposition:** CONFIRMED (requires catalog verification in Phase 6)
**Evidence:** Browser smoke report shows baseline-only campaign. Node benchmark has 18 matchups including HYBRIX policies. Phase 6 will establish the authoritative catalog.

### Claim 16: Fixed CI stage total disagreed with actual run_step calls

**Disposition:** CONFIRMED
**Evidence:** `scripts/ci.sh` has 33 `run_step` invocations but prints `CI PASS: %s/32 stages`.

### Claim 17: Default test/CI omitting two anchor suites

**Disposition:** CONFIRMED
**Evidence:** `package.json` `test` script lists 21 test files. `scripts/ci.sh` lists 21 test steps. Both omit `v0.10.0-anchor-authority.test.mjs` and `v0.10.0-browser-anchor-parity.test.mjs`. Directory has 23 test files.

### Claim 18: CI verifying inherited browser reports without freshly generating them

**Disposition:** CONFIRMED
**Evidence:** CI steps `browser-ui-report` and `browser-parity-report` call `verify-browser-ui-report.mjs` and `verify-browser-parity-report.mjs` which read existing JSON reports. No preceding step calls `browser-ui-smoke.mjs` or `browser-parity.mjs` to generate fresh reports.

### Claim 19: Browser smoke JSON/Markdown/test disagreement about workspace count

**Disposition:** CONFIRMED
**Evidence:** JSON has 7 workspace keys, MD says "Eight smoke-tested workspaces", test expects 10. Actual app.js defines 11 workspace routes.

### Claim 20: Competing obsolete release workflows and platform-specific assumptions

**Disposition:** CONFIRMED
**Evidence:** `scripts/package-release.mjs` (expects `release/` to exist, uses `deterministic_zip.py`) vs `scripts/generate-release.mjs` (creates `release/`, different zip name). Browser scripts hardcode `/usr/lib/chromium/chromium` (Linux path). Windows Chrome cleanup fails EBUSY.

### Claim 21: Self-audit PASS/94 contradicted by red gates and privacy leakage

**Disposition:** CONFIRMED
**Evidence:** `reports/self-audit.json` claims `status: PASS, score: 94` with all critical gates false. Contradicted by: 3 test failures, missing release directory, privacy leak (DP identities + rng in player views), accounting invariant false, stale browser proof verifiers.

## Conflict ledger

| Conflict | Authority | Decision |
|---|---|---|
| Self-audit PASS vs executable red gates | Executable behavior (4) > Generated reports (7) | Self-audit REJECTED; executable gates authoritative |
| Stale verifier expects Engine 4.2.4 vs actual 4.2.5 | Engine manifest (3) > Tests (6) | Verifier must be corrected to 4.2.5/4.1.2 |
| Smoke MD says 8 vs JSON has 7 vs test expects 10 | App.js source (5) defines 11 routes | All three must agree on actual workspace count |
