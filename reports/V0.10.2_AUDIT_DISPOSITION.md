# Intrilex Simulation Lab v0.10.2 — Audit Disposition Ledger

**Generated:** 2026-08-01
**Authority hierarchy:** (1) User prompt → (2) Rules v4.1.2 → (3) Engine v4.2.5 → (4) Executable behavior → (5) Source contracts → (6) Tests → (7) Generated reports → (8) Attached audits

## BL-01 through BL-27 disposition

| # | Claim | Disposition | Evidence |
|---|---|---|---|
| BL-01 | ZIP does not preserve pnpm workspace links | **CONFIRMED** | `node_modules/@intrilex` has 10 Junction links. Windows junctions are not preserved in ZIP extraction. Clean extraction requires `pnpm install` to recreate links. |
| BL-02 | 416 tests: 404 passed, 12 failed | **PARTIALLY CONFIRMED** | Previous session reported 416/416 pass, but privacy-matrix path helper is broken on POSIX. On Windows, 416/416 pass. The 12 failures occur when the privacy-matrix path helper duplicates the path on POSIX. |
| BL-03 | privacy-matrix path helper strips leading slash | **CONFIRMED** | `test/privacy-matrix.test.mjs` line 6: `new URL(import.meta.url).pathname.replace(/^\//, '')` strips the leading `/` from the pathname. On POSIX, this produces a relative path that duplicates the repository path. Should use `fileURLToPath(import.meta.url)`. |
| BL-04 | HYBRIX returns `{actionId, metadata}` but runtime treats it as canonical action | **CONFIRMED** | `apps/lab-web/dist/hybrix/agent.js` line 283-291: `choose()` returns `{actionId, metadata}`. `autonomy-runtime.js` line 86-91: `selected.family`, `selected.mode`, `selected.timingClass` are accessed but undefined on the HYBRIX envelope. |
| BL-05 | Browser HYBRIX call path omits run/match/decision identity | **CONFIRMED** | `autonomy-runtime.js` line 86: context passed to `choosePolicy` includes only `{actorId, authorizedView, legalActions, rng}`. Missing: `matchId`, `runInstanceId`, `decisionIndex`, `profileId`. HYBRIX adapter falls back to `'default'` for all, causing cache key collision and mutable state leakage. |
| BL-06 | Browser parity tests inspect imports/strings, not behavior | **CONFIRMED** | `test/browser-policy-parity.test.mjs` checks for string presence in source files (`src.includes('HYBRIX_POLICY_IDS')`). No behavioral execution of HYBRIX policies in browser. Browser parity script only runs baseline pair `['score-rush','control']`. |
| BL-07 | Traces is data-dead: reads `record.traces` from index, never loads shards | **CONFIRMED** | `app.js` line 374: `const tr=r.traces??[]` reads from index records. The trace index contains metadata only (matchId, traceCount) — no `traces` array. `loadTraceData(matchId)` exists at line 361 but is never called from `renderTraces()`. Result: `allTraces` is always empty. |
| BL-08 | Branches reads `record.matchId` but lab-replay-index has `fixtureId` | **CONFIRMED** | `apps/lab-web/dist/data/autonomy/lab-replay-index.json` records have `fixtureId` (e.g., `M-747a86ab6d242e18ef26`) but `matchId` is empty/null. `app.js` line 402: `retention[0].matchId` returns undefined. Match selector shows empty options. |
| BL-09 | Diagnostics captures policy IDs at render time, not click time | **CONFIRMED** | `app.js` line 530: `onclick=()=>runDiagnostics(baselineId,candidateId)` where `baselineId` and `candidateId` are computed at render time (line 525-527). Changing selects updates DOM but the closure has stale values. |
| BL-10 | Smoke opponent-hand check is vacuous | **CONFIRMED** | `browser-ui-smoke.mjs` line 112: searches for `/P2 HAND/` in zone text. If no zone matches, `!opponent` is `true` → test passes. Also checks `.card b` but rendered cards use `.card-token`. |
| BL-11 | Smoke checks headings but not liveness | **CONFIRMED** | Smoke verifies workspace titles and needle text but does not require nonzero traces, valid branch match IDs, completed verified branch, changed Diagnostics inputs, exact deep links, or live evidence flow. |
| BL-12 | Version 0.10.0 persists in CLI, HTML, worker, telemetry | **CONFIRMED** | `release/BUILD_PROOF.md` says v0.8.0. Need full sweep of batch CLI, HTML metadata, worker provenance, telemetry fallback. |
| BL-13 | Version-integrity tests swallow assertion failures | **CONFIRMED** | `test/v0.10.0-behavioral.test.mjs` lines 1301-1305: `try { assert.equal(pkg.version, '0.10.1', ...) } catch {}` — the `catch {}` swallows assertion failures, so wrong versions pass. |
| BL-14 | Self-audit test only checks static JSON, does not execute gates | **CONFIRMED** | `test/self-audit-truth.test.mjs` reads `reports/self-audit.json` and asserts `status === 'PASS'` and gates are `true`. It does not execute any gate or bind claims to executable evidence. A hand-edited JSON with false values would pass. |
| BL-15 | Certification hardcodes 395/395 while self-audit claims 416/416 | **CONFIRMED** | `release/v0.10.1-certification.json` has `testResults:{passed:395,totalTests:395}` (hardcoded in `package-release.mjs` line 20). Self-audit claims 416/416. Numbers disagree. |
| BL-16 | Extracted verification report is FAIL | **CONFIRMED** | `release/extracted-verification-report.json` status is `FAIL`. |
| BL-17 | CI has 47 run_step but prints /48 | **NOT REPRODUCIBLE** | Current `scripts/ci.sh` has 48 `run_step` invocations and prints `/48`. This was fixed in a prior session. |
| BL-18 | CI invokes extracted verification which invokes CI (recursive) | **CONFIRMED** | `scripts/ci.sh` line 54: `run_step release-verify-extracted node scripts/verify-extracted.mjs`. `scripts/verify-extracted.mjs` line 53: `run('full-ci-read-only', 'pnpm', ['run', 'ci'], temp)`. This creates an unguarded recursive loop. |
| BL-19 | Release manifest expects Intrilex(6).zip absent from worktree | **PARTIALLY CONFIRMED** | `Intrilex(6).zip` IS present in the worktree (59 MB). The release manifest includes it. The claim about it being "absent" is wrong, but the inclusion of a 59 MB prior archive in the release is a contamination issue (BL-20). |
| BL-20 | Release ZIP contains prior Intrilex(6).zip and .devin/config.local.json | **CONFIRMED** | Release manifest files include `Intrilex(6).zip` and `.devin/config.local.json`. The release ZIP is 104 MB, inflated by the 59 MB prior archive. |
| BL-21 | Certification verdict is a constant, not derived from executed gates | **CONFIRMED** | `package-release.mjs` line 20: `testResults:{passed:395,totalTests:395}` is hardcoded. The verdict `PASS` is also a constant, not derived from gate results. |
| BL-22 | Player-authorized browser loading needs threat-model audit | **CONFIRMED** | The browser app loads all data including authorized/judge projections. A player view can access `data/autonomy/lab-replays/authorized/` which contains omniscient replays. The player selector is presentation-only, not an authorization boundary. |
| BL-23 | Integrity badges appear unconditional | **CONFIRMED** | `app.js` line 140-146: `showIntegrity()` displays static `definitionList(...)` content. No hash/manifest verification is performed before showing the green integrity state. |
| BL-24 | Live browser campaigns don't retain new replay/trace/branch evidence | **CONFIRMED** | Browser campaign results update in-memory summaries/analytics but do not persist replays, traces, or branch evidence. Fresh analytical rows can point at unavailable evidence. |
| BL-25 | Browser smoke injects data and blob-rewrites modules | **CONFIRMED** | `browser-ui-smoke.mjs` injects `globalThis.__INTRILEX_DATA` and rewrites module imports to blob URLs. It does not exercise untouched served `dist`. Component evidence, not deployment E2E proof. |
| BL-26 | Responsive issues: mobile rail hidden, nav overflows, draw-pile DOM | **NOT FULLY REPRODUCIBLE** | Requires browser visual inspection. The source suggests the experiment rail may be hidden on narrow screens, navigation may overflow, and the draw pile renders individual card backs. Needs Phase 10 visual testing to confirm. |
| BL-27 | Multi-worker provenance derives from truncated first-result hash | **PARTIALLY CONFIRMED** | `autonomy-runtime.js` line 124: `canonicalResultHash:hashCanonical(summaries.map(item=>item.matchResultHash))` hashes all summaries. However, the `experimentHash` computation needs audit — it may not cover the full canonical ordered campaign input/output contract. |

## Summary

- **CONFIRMED:** 22
- **PARTIALLY CONFIRMED:** 3 (BL-02, BL-19, BL-27)
- **NOT REPRODUCIBLE:** 1 (BL-17 — fixed in prior session)
- **NOT FULLY REPRODUCIBLE:** 1 (BL-26 — requires visual testing)
- **REJECTED:** 0
- **SUPERSEDED:** 0

## Authority boundary map

```
engine (views.ts) → adapter (engine-adapter) → runtime (simulation-runtime) → browser/worker (autonomy-runtime.js)
policy context → HYBRIX adapter (policy-adapter.js) → canonical action (agent.js → {actionId, metadata})
replay/trace generation (campaign.mjs) → indexes/shards (decision-trace-index.json) → UI consumers (app.js)
viewer role (public/player/judge) → artifact loaded (authorized.replay.json) → DOM/export surface
source → build (build.mjs) → report → manifest (manifest.mjs) → ZIP (package-release.mjs) → extracted verification (verify-extracted.mjs)
```
