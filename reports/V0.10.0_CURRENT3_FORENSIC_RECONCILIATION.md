> **⚠ TRUTH QUARANTINE (Phase 0):** This report was produced by the pre-Cascade or
> in-Cascade validator which contained the Section 3.3 anchor inversion
> (decisionId re-derivation + actor/seat presence-only check). Its PASS verdicts
> on counterfactual anchor binding are **HISTORICAL/PROVISIONAL** and not
> authoritative. The authoritative baseline is `Intrilex(4-5).zip`
> (SHA-256 `CCC2D4AF...AFD9`), verified at commit `81ce617`. The anchor inversion
> was fixed at commit `8f239ee`. See `V0.10.0_IMPLEMENTATION_LEDGER.md`.

# V0.10.0 Current3 Forensic Reconciliation

**Date:** 2026-07-30  
**Engineer:** GLM-5.2  
**Baseline:** current3 (Intrilex Simulation Lab, provisional v0.10.0)

## 1. Environment

- **OS:** Windows
- **Node:** current LTS
- **Package manager:** pnpm (frozen lockfile works; npm lockfile stale at v0.8.0)
- **Packages:** 12 workspace packages, zero active cycles
- **Test baseline:** 211/211 after `pnpm install --offline --frozen-lockfile`

## 2. Verified Findings

| # | Finding | Verified Status | Evidence |
|---|---------|----------------|----------|
| 5.1 | Genuine improvements exist | CONFIRMED | src/dist hashes match; campaign reports 0.10.0; traces use 2.0.0; 211/211 pass |
| 5.2 | Extracted archive not runnable as shipped | CONFIRMED | npm test without pnpm install = 11 failures from missing workspace links |
| 5.3 | npm installation broken | CONFIRMED | `npm ci --offline` fails EUNSUPPORTEDPROTOCOL; package-lock.json at v0.8.0 |
| 5.4 | Browser Branch Lab cannot run real branch | CONFIRMED | `app.js:406-416` sends matchId/checkpointHash/altActionId but no replay; UI has free-text action ID, raw checkpoint index, independent A/B buttons |
| 5.5 | Node decision anchoring unbound | CONFIRMED | `counterfactual.mjs:26-33` accepts caller matchId/checkpointHash without validation; no decisionId binding; no selected action verification |
| 5.6 | Counterfactual not one paired experiment | CONFIRMED | `counterfactual.mjs` runs one action per call; UI calls independently for A and B |
| 5.7 | Contract suite passes without proving contract | CONFIRMED | 32/32 pass but anchor tests use truncated hashes; audit tests accept truthy strings; no browser Branch/Diagnostics E2E |
| 5.8 | Decision Evidence v2 causally misleading | CONFIRMED | policy-sourced scores exempt from reconciliation; UI shows "Score decomposition" with zero components beside actual total |
| 5.9 | Rule audits ceremonial | CONFIRMED | checks lack expected/evidenceRefs; browser reads `c.check` while source emits `checkId`; overall can PASS with all NOT_APPLICABLE |
| 5.10 | Browser Diagnostics has zero decision evidence | CONFIRMED | `app.js:456` sends `state.observatory?.decisions ?? []`; observatory has no decisions field |
| 5.11 | HYBRIX trace transport broken | CONFIRMED | `runtime.mjs:251` preserves reasonCode + candidateScores but discards metadata.hybrixTrace |
| 5.12 | HYBRIX run isolation broken | CONFIRMED | `policy-adapter.mjs:61` caches by `matchId:actorId`; no runInstanceId; identical configs produce different results across runs |
| 5.13 | HYBRIX not fully Intrilex-native | CONFIRMED | `agent.mjs` imports coordination blackboard, failsafe, LOD; `tick()` uses spatial concepts; lines 589-590 map to ATTACK |
| 5.14 | Benchmark accounting invalid | CONFIRMED | `benchmark-hybrix.mjs:125-126` counts seat wins as policy wins; line 144 counts errors as draws; self-play enters aggregate |
| 5.15 | Campaign failures filtered out | CONFIRMED | `campaign.mjs:61` filters `records.filter(r => r.result)` — errored matches silently dropped |
| 5.16 | Version and certification drift | CONFIRMED | README at v0.8.0; package-lock.json at v0.8.0; no packageManager field; browser parity/UI verifiers expect v0.8.0 |
| 5.17 | No valid v0.10.0 release exists | CONFIRMED | No v0.10.0-certification.json; no release ZIP; no SHA-256 companion; no extracted verification report |

## 3. Treatment Plan

Each finding maps to a phase in the Required Implementation Program:

- 5.2, 5.3, 5.16 → Phase 2 (package path) + Phase 11 (version alignment)
- 5.4, 5.5, 5.6 → Phase 5 (paired counterfactual API) + Phase 6 (browser Branch UX)
- 5.7 → Phase 1 (red behavioral tests)
- 5.8, 5.11 → Phase 3 (decision evidence)
- 5.9 → Phase 4 (rule audits)
- 5.10 → Phase 7 (diagnostics pipeline)
- 5.12, 5.13 → Phase 8 (HYBRIX native + isolation)
- 5.14 → Phase 9 (benchmark rebuild)
- 5.15 → Phase 10 (campaign reliability)
- 5.17 → Phase 13 (packaging + certification)

## 4. Phase 0 Actions (Truth Quarantine)

1. ✅ Forensic report created (this document)
2. Mark inherited PASS reports as historical/provisional
3. Disable browser Branch completion claims → show unavailable state
4. Make Diagnostics show "Decision evidence unavailable"
5. Remove invalid HYBRIX aggregate claims from user-facing surfaces
6. Create implementation ledger
